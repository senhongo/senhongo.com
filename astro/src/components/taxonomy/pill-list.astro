---
import classnames from 'classnames';
import styles from './taxonomy.module.scss';

interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<div class={classnames(styles.pillList, className)}>
  <slot />
</div>
